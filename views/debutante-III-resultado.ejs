<% include _header %>
<div class="container">
    <div class="jumbotron">
        <h1>Terminaste Débutante III</h1>
        <p class="lead">Concluiste correctamente el nivel de Debutante III</p>
        <div class="form-inline">
            <label><b>Resultado: </b></label>
            <label> <%= currentUsuario.deb3 %></label>
            <label>/20</label>
        </div>
        <% if(currentUsuario.deb3 >= 14) {%> 
            <a class="btn btn-outline-success" href="/intermedio-I">Continuar</a>
        <% } else {%>
            <a class="btn btn-outline-danger" href="/etudiantes/<%= currentUsuario.username %>">Finalizar</a>
        <% } %>
    </div>
</div>
<% include _footer %>