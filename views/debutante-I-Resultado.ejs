<% include _header %>
<div class="container">
    <div class="jumbotron">
        <h1>Terminaste Débutante I</h1>
        <div class="form-inline">
            <label><b>Resultado: </b></label>
            <label> <%= currentUsuario.deb1 %></label>
            <label>/35</label>
        </div>
        <% if(currentUsuario.deb1 >= 24) {%> 
            <a class="btn btn-outline-success" href="/debutante-II">Continuar</a>
        <% } else {%>
            <a class="btn btn-outline-danger" href="/etudiantes/<%= currentUsuario.username %>">Finalizar</a>
        <% } %>
    </div>
</div>
<% include _footer %>