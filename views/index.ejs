<% include _header %>
<div class="container">
    <% if(currentUsuario && currentUsuario.role !== "admin") { %>
        <% if(currentUsuario.resultado) { %> 
            <div class="jumbotron">
                <a class="btn btn-outline-success btn-lg" href="/etudiantes/<%= currentUsuario.username %>">Mi nivel de ubicación</a>
            </div>
        <% } else { %>
            <div class="jumbotron">
                <h2>¡Cuidado!</h2>
                <p class="lead">Una vez comenzado el examen solo se podrá presentar una vez, de tal modo se evita que se pueda hacer trampa o repetir el mismo examen dos veces.</p>
                <a class="btn btn-outline-danger btn-lg" href="/inicio-examen">Iniciar examen</a>
            </div>
        <% } %>
    <% } else if(!currentUsuario){ %>
        <div class="container">
            <div class="jumbotron">
            <h1>Bienvenido!</h1>
            <h4>Por favor sigue las instrucciones a continuación</h4>
            <ol class="lead">
                <li>Registrarse en el sistema. <i>Cuida la información que pones debido a que no podrá ser editada.</i></li>
                <li>Iniciar sesión con tu usuario y contraseña.</li>
                <li>Te aparecerá el enlace para realizar el examen, da clic para realizarlo.</li>
                <li>El examen se detendrá hasta el nivel que hayas podido llegar.</li>
            </ol>
            <h5>Bonne chance!</h5>
            <br>
                <div class="row" style="text-align:center;">
                    <div class="col-md-5">
                        <img src="/imgs/studentg.png" width="110" height="110">  
                        <a class="btn btn-primary btn-lg" href="/signup">Registro</a>
                    </div>
                    <div class="col-md-5">
                        <a class="btn btn-success btn-lg" href="/login">Iniciar sesión</a>
                        <img src="/imgs/studentb.png" width="110" height="110">
                    </div>
                </div>
            </div>
        </div>
    <% } else if(currentUsuario && currentUsuario.role === "admin") { %>
        <div class="jumbotron">
            <h1>Bienvenido administrador!</h1>
        </div>
    <% } %>
</div>
<% include _footer %>